spring:
  application:
    name: phenikaa-course-management

  datasource:
    url: jdbc:mysql://localhost:3306/ojt_team5?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        show_sql: true
        use_sql_comments: true
        generate_statistics: false
        connection:
          characterEncoding: utf8mb4
          CharSet: utf8mb4
          useUnicode: true
    open-in-view: false

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.phenikaauniversity.coursemanagement: DEBUG
jwt:
  expire: 3600
  refresh: 7200

cloudinary:
  cloud_name: djppquc7s
  api_key: 795681177339359
  api_secret: KodU8NiCXli1PedjAOq8yMNDXzk